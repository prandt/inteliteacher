import 'package:flutter/cupertino.dart';

class ClassPlanItem extends StatelessWidget {
  const ClassPlanItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
